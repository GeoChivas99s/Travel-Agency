import {
  BorderProps,
  DisplayProps,
  FlexboxProps,
  LayoutProps,
  PositionProps,
  ShadowProps,
  SpaceProps,
} from 'styled-system';

export interface ImageProps
  extends SpaceProps,
    DisplayProps,
    BorderProps,
    PositionProps,
    FlexboxProps,
    LayoutProps,
    ShadowProps {}
