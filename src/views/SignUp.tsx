import React, { FC } from 'react';

import { Layout, SignUp } from '../components';

const SignUpView: FC = () => (
  <Layout pageTitle="Registar-se">
    <SignUp />
  </Layout>
);

export default SignUpView;
